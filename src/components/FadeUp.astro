---
interface Props {
  delay?: number;
  as?: keyof HTMLElementTagNameMap;
  class?: string;
}

const { delay = 0, as: Tag = "div", class: className = "" } = Astro.props;
---

<Tag class:list={["fade-up", className]} data-delay={delay}>
  <slot />
</Tag>
